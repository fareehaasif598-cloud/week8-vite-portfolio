import { defineConfig } from 'vite';

export default defineConfig({
  // REQUIRED for GitHub Pages project sites (e.g. username.github.io/repo-name/).
  // Uncomment the line below and replace 'your-repo-name' with your actual
  // repository name before running `npm run build` for deployment.
  // Skip this if you're deploying to a custom domain, Vercel, or Netlify.

  // base: '/your-repo-name/',
});
